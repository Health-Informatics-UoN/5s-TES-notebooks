# Contingency tables notebook

Five Safes TES can be used to build contingency tables from federated data.
These can then be used with Scipy's functions for statistics on contingency tables.

Run the notebook with:
```bash
uv run jupyter notebook
```

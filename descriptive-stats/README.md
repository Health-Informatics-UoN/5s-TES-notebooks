# Descriptive statistics notebook

Five Safes TES can be used to calculate the summary statistics sufficient to federate many analyses.
This notebook shows an example of calculating summary statistics from federated intermediate values.

Run the notebook with:
```bash
uv run jupyter notebook
```

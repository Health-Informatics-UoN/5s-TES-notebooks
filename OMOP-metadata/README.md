# OMOP metadata notebook

Five Safes TES can be used to gather metadata on the OMOP datasets held in TREs.
This notebook shows an example of visualisations that can be used to explore this metadata.

Run the notebook with:
```bash
uv run jupyter notebook
```
